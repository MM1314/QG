@model IEnumerable<SOA.WLIMS.Service.DAL.Order>

@{
    ViewBag.Title = "Index";
}

<h2>订单管理</h2>

<p>
    @Html.ActionLink("新增", "Create")
</p>
<table>
    <tr>
        
        <th>
            Code
        </th>
        <th>
            SrcUserName
        </th>
        <th>
            Contents
        </th>
        <th>
            ContentsWeight
        </th>
        <th>
            ContentsValue
        </th>
        <th>
            ToUserName
        </th>
        <th>
            Status
        </th>
        <th>
            Charges
        </th>
        <th>
            PickupTime
        </th>
        <th>
            SigninTime
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @item.Code
        </td>
        <td>
            @item.SrcUserName
        </td>
        <td>
            @item.Contents
        </td>
        <td>
            @String.Format("{0:F}", item.ContentsWeight)
        </td>
        <td>
            @String.Format("{0:F}", item.ContentsValue)
        </td>
        <td>
            @item.ToUserName
        </td>
        <td>
            @item.Status
        </td>
        <td>
            @String.Format("{0:F}", item.Charges)
        </td>
        <td>
            @String.Format("{0:g}", item.PickupTime)
        </td>
        <td>
            @String.Format("{0:g}", item.SigninTime)
        </td>
        
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>

